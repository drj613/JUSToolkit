# Names that look like bead ids but are gdb / script commands.
# check_docs.py cannot tell them apart: a real bead id ends in a 3-char hash,
# and these happen to end in three characters too.
jus-auto-snapshot-on-hit
jus-auto-snapshot-off
